{
  "name": "cybergrill",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Game night scoreboard \u2014 arcade leaderboard, cup tournaments, UNO room, and a shareable recap card per night.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check",
    "cards": "node scripts/render-card.mjs",
    "all": "npm run build && npm run cards",
    "passcode": "node scripts/passcode.mjs",
    "night": "node scripts/night.mjs",
    "test": "node game/engine.test.mjs && node game/ludo/engine.test.mjs && node game/ronda/engine.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}